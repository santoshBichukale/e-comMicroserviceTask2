spring:
  application:
    name: UserService

  datasource:
    url: jdbc:mysql://localhost:3306/taskmicro3?createDatabaseIfNotExist=true
    username: root
    password: Santosh123@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 9091

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
